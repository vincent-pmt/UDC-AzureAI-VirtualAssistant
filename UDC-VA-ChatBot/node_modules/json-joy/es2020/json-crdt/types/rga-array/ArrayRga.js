"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.ArrayRga = exports.ArrayChunk = void 0;
const AbstractRga_1 = require("../rga/AbstractRga");
const clock_1 = require("../../../json-crdt-patch/clock");
const printBinary_1 = require("../../../util/print/printBinary");
const printTree_1 = require("../../../util/print/printTree");
const Empty = [];
class ArrayChunk {
    constructor(id, span, data) {
        this.id = id;
        this.span = span;
        this.len = data ? span : 0;
        this.del = !data;
        this.p = undefined;
        this.l = undefined;
        this.r = undefined;
        this.s = undefined;
        this.data = data;
    }
    merge(data) {
        this.data.push(...data);
        this.span = this.data.length;
    }
    split(ticks) {
        const span = this.span;
        this.span = ticks;
        if (!this.del) {
            const data = this.data;
            const rightData = data.splice(ticks);
            const chunk = new ArrayChunk((0, clock_1.tick)(this.id, ticks), span - ticks, rightData);
            return chunk;
        }
        return new ArrayChunk((0, clock_1.tick)(this.id, ticks), span - ticks, undefined);
    }
    delete() {
        this.del = true;
        this.data = undefined;
    }
    clone() {
        return new ArrayChunk(this.id, this.span, this.data ? [...this.data] : undefined);
    }
}
exports.ArrayChunk = ArrayChunk;
class ArrayRga extends AbstractRga_1.AbstractRga {
    constructor(doc, id) {
        super(id);
        this.doc = doc;
        this._view = Empty;
    }
    create() {
        return new ArrayRga(this.doc, this.id);
    }
    createChunk(id, data) {
        return new ArrayChunk(id, data ? data.length : 0, data);
    }
    onViewChange() {
        this._view = Empty;
    }
    view() {
        const arr = [];
        let chunk = this.first();
        const index = this.doc.index;
        while (chunk) {
            if (!chunk.del)
                for (const node of chunk.data)
                    arr.push(index.get(node).view());
            chunk = this.next(chunk);
        }
        const _view = this._view;
        if (arr.length !== _view.length)
            return (this._view = arr);
        for (let i = 0; i < arr.length; i++)
            if (arr[i] !== _view[i])
                return (this._view = arr);
        return _view;
    }
    children(callback) {
        const index = this.doc.index;
        for (let chunk = this.first(); chunk; chunk = this.next(chunk))
            if (!chunk.del)
                for (const node of chunk.data)
                    callback(index.get(node));
    }
    get(position) {
        const pair = this.findChunk(position);
        if (!pair)
            return undefined;
        return pair[0].data[pair[1]];
    }
    getNode(position) {
        const id = this.get(position);
        if (!id)
            return undefined;
        return this.doc.index.get(id);
    }
    getById(id) {
        const chunk = this.findById(id);
        if (!chunk || chunk.del)
            return undefined;
        const offset = id.time - chunk.id.time;
        return chunk.data[offset];
    }
    child() {
        return undefined;
    }
    container() {
        return this;
    }
    printChunk(tab, chunk) {
        const pos = this.pos(chunk);
        let valueTree = '';
        if (!chunk.del) {
            const index = this.doc.index;
            valueTree = (0, printTree_1.printTree)(tab, chunk.data.map((id, i) => (tab) => `[${pos + i}]: ${index.get(id).toString(tab + '    ' + ' '.repeat(String(i).length))}`));
        }
        return (this.formatChunk(chunk) +
            valueTree +
            (0, printBinary_1.printBinary)(tab, [
                chunk.l ? (tab) => this.printChunk(tab, chunk.l) : null,
                chunk.r ? (tab) => this.printChunk(tab, chunk.r) : null,
            ]));
    }
}
exports.ArrayRga = ArrayRga;
