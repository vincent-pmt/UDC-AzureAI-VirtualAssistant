import { type ITimestampStruct } from '../../../json-crdt-patch/clock';
import type { JsonNode } from '../types';
import type { Printable } from '../../../util/print/types';
export declare class Const implements JsonNode, Printable {
    readonly id: ITimestampStruct;
    readonly val: unknown | ITimestampStruct;
    constructor(id: ITimestampStruct, val: unknown | ITimestampStruct);
    view(): unknown;
    children(): void;
    child(): undefined;
    container(): JsonNode | undefined;
    toString(tab?: string): string;
}
