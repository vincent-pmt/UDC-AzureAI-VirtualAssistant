import { ITimestampStruct } from '../../../json-crdt-patch/clock';
import type { JsonNode } from '../../types';
import type { Model } from '../../model';
import type { Printable } from '../../../util/print/types';
export declare class ValueLww implements JsonNode, Printable {
    readonly doc: Model;
    readonly id: ITimestampStruct;
    val: ITimestampStruct;
    constructor(doc: Model, id: ITimestampStruct, val: ITimestampStruct);
    set(val: ITimestampStruct): ITimestampStruct | undefined;
    node(): JsonNode;
    view(): unknown;
    children(callback: (node: JsonNode) => void): void;
    child(): JsonNode;
    container(): JsonNode | undefined;
    toString(tab?: string): string;
}
