import { decode as decodeCompact } from '../compact/decode';
import { decode as decodeMsgPack } from '../../../json-pack/msgpack/util';
export const decode = (buf) => {
    return decodeCompact(decodeMsgPack(buf));
};
