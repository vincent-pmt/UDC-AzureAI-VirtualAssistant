import { toDisplayString } from '../clock';
export class StrOp {
    id;
    constructor(id) {
        this.id = id;
    }
    span() {
        return 1;
    }
    name() {
        return 'str';
    }
    toString() {
        return `"${this.name()}" ${toDisplayString(this.id)}`;
    }
}
