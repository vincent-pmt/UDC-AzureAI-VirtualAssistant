import { toDisplayString } from '../clock';
export class BinOp {
    id;
    constructor(id) {
        this.id = id;
    }
    span() {
        return 1;
    }
    name() {
        return 'bin';
    }
    toString(tab = '') {
        return `"${this.name()}" ${toDisplayString(this.id)}`;
    }
}
