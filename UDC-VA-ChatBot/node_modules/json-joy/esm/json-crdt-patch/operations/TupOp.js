import { toDisplayString } from '../clock';
export class TupOp {
    id;
    constructor(id) {
        this.id = id;
    }
    span() {
        return 1;
    }
    name() {
        return 'tup';
    }
    toString() {
        return `"${this.name()}" ${toDisplayString(this.id)}`;
    }
}
