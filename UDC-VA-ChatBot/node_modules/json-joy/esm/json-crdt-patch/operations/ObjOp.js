import { toDisplayString } from '../clock';
export class ObjOp {
    id;
    constructor(id) {
        this.id = id;
    }
    span() {
        return 1;
    }
    name() {
        return 'obj';
    }
    toString() {
        return `"${this.name()}" ${toDisplayString(this.id)}`;
    }
}
