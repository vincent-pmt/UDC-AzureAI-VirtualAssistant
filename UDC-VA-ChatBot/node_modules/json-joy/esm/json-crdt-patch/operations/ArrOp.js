import { toDisplayString } from '../clock';
export class ArrOp {
    id;
    constructor(id) {
        this.id = id;
    }
    span() {
        return 1;
    }
    name() {
        return 'arr';
    }
    toString() {
        return `"${this.name()}" ${toDisplayString(this.id)}`;
    }
}
