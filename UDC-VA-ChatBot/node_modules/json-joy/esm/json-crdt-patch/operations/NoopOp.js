import { toDisplayString } from '../clock';
export class NoopOp {
    id;
    len;
    constructor(id, len) {
        this.id = id;
        this.len = len;
    }
    span() {
        return this.len;
    }
    name() {
        return 'noop';
    }
    toString() {
        return `"${this.name()}" ${toDisplayString(this.id)}!${this.len}`;
    }
}
