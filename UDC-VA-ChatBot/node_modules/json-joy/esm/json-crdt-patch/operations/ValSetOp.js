import { toDisplayString } from '../clock';
export class ValSetOp {
    id;
    obj;
    val;
    constructor(id, obj, val) {
        this.id = id;
        this.obj = obj;
        this.val = val;
    }
    span() {
        return 0;
    }
    name() {
        return 'val_set';
    }
    toString(tab = '') {
        return `"${this.name()}" ${toDisplayString(this.id)}!${this.span()}, obj = ${toDisplayString(this.obj)}, val = ${toDisplayString(this.val)}`;
    }
}
