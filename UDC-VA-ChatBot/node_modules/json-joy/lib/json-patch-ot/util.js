"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.replaceIndices = void 0;
var json_pointer_1 = require("../json-pointer");
function replaceIndices(path, arrayPath, index, incUp) {
    var remainder = path.substr(arrayPath.length);
    var slashIndex = remainder.indexOf('/');
    if (slashIndex === -1)
        slashIndex = remainder.length;
    var oldIndex = remainder.substr(0, slashIndex);
    var rest = remainder.substr(slashIndex);
    var isOldBigger = incUp ? oldIndex >= index : oldIndex > index;
    var shouldChangeIndex = (0, json_pointer_1.isValidIndex)(oldIndex) && isOldBigger;
    return shouldChangeIndex ? "".concat(arrayPath).concat(~~oldIndex + (incUp ? 1 : -1)).concat(rest) : path;
}
exports.replaceIndices = replaceIndices;
