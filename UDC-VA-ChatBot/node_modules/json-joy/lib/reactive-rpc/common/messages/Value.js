"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.Value = void 0;
var Value = (function () {
    function Value(data, type) {
        this.data = data;
        this.type = type;
    }
    Value.prototype.encode = function (codec) {
        var value = this.data;
        var type = this.type;
        if (value === undefined)
            return;
        var encoder = codec.encoder;
        if (!type)
            encoder.writeAny(value);
        else
            type.encoder(codec.format)(value, encoder);
    };
    return Value;
}());
exports.Value = Value;
