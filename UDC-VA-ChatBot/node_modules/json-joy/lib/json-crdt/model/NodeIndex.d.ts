import type { Identifiable } from '../../json-crdt-patch/Identifiable';
import { ITimestampStruct } from '../../json-crdt-patch/clock';
export declare class NodeIndex<T extends Identifiable> {
    entries: Map<number, Map<number, T>>;
    get(id: ITimestampStruct): undefined | T;
    set(operation: T): void;
    delete(id: ITimestampStruct): void;
    forEach(callback: (node: T) => void): void;
    toString(tab?: string): string;
}
